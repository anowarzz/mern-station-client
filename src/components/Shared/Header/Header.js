import React from 'react';

const Header = () => {
    return (
        <div>
            h3.text-3xl{
                This is the header section
            }
        </div>
    );
};

export default Header;